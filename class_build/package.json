{
  "name": "class_build",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:dev:ssg": "cp .env-dev .env && next build && next export",
    "build:loacl:ssg": " cp .env-local .env && next build && next export",
    "build:prod:ssg": "cp .env-prod .env && next build && next export",
    "build:stage:ssg": "cp .env-stage .env && next build && next export",
    "build:dev:ssr": "cp .env-dev .env && next build",
    "build:local:ssr": "cp .env-local .env && next build",
    "build:prod:ssr": "cp .env-prod .env && next build",
    "build:stage:ssr": "cp .env-stage .env && next build",
    "dev": "cp .env-local .env && next dev",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "graphql": "^15.5.1",
    "graphql-request": "^3.5.0",
    "next": "11.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.0"
  }
}
